server:
  port: 8083
spring:
  application:
    name: product
  datasource:
    url: jdbc:mysql://localhost:3306/repast_product?serverTimezone=UTC&allowMultiQueries=true
    username: root
    password: 159852
  jpa:
    generate-ddl: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true